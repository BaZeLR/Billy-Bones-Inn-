# RPY Audit (2026-03-06)

> REFERENCE ONLY / HISTORICAL AUDIT.
>
> Do not use this as implementation authority. Current architecture is defined
> by `devdocs/README.md` and the model/template docs.

Scope: full `game/Inn/*.rpy` pass for runtime hazards and flow break risks.

## Fixed In This Pass

1. `game/Inn/NextDay_FinishDayEvents.rpy`
- Rewrote `next_day_finish_day_events()` to use `renpy.store` state access.
- Removed local-shadowing bug that could reset core dictionaries each call.
- Replaced unsafe direct key reads in critical branches with safe `.get()`/defaults where needed.
- Hardened day-finish path for partially initialized saves.

2. `game/Inn/MarketPlace.rpy`
- Added `default KnowMongol = 0`.
- Added defensive `MongolVar` initialization and key defaults (`HorsePrice`, `ZimmerKnow`, `DiscountAsk`).
- Prevents startup `NameError/KeyError` in market flow.

3. `game/Inn/DisplayTavernEventShort.rpy`
- Added Python compatibility function `DisplayTavernEventShort(time_period, eyewitness)`.
- Added script alias label `DisplayTavernEventShort(...)`.
- Unifies debug/test calls with the same dispatcher used by gameplay.

4. `game/Inn/CharacterActionHub.rpy`
- Removed Python-side `renpy.call(...)` from talk action.
- Switched to script-level `call expression ... pass ...` for label invocation.

## Current Hotspot Metrics (`game/Inn/*.rpy`)

- `renpy.call_in_new_context(...)`: `0`
- `renpy.call(...)` (Python API invocations): `251`
- `renpy.jump(...)` (Python API invocations): `1`
- `globals(...)` usages: `1860`
- direct `Var['key']` accesses: `287`

These are not all bugs by themselves, but they are the primary risk surface for save/load instability and flow desync.

## Priority Remaining Refactor Targets

1. `game/Inn/DebugTools.rpy`
- still uses Python `renpy.call*` heavily; keep for debug only or migrate to label-driven wrappers.

2. `game/Inn/onobjsel.rpy`
- object dispatch already moved off `call_in_new_context`; further cleanup is optional.

3. `game/Inn/AmandaDynamicCommonBlocks.rpy`
- dynamic jump paths migrated; monitor runtime behavior in tavern flow tests.

4. table subsystem files (`Table*.rpy`, `TableSupport.rpy`)
- heavy `globals()` dynamic storage; keep as compatibility layer for now, then isolate behind typed helper API.

## Notes

- Main UI/layout was not structurally changed in this pass.
- This audit pass focused on runtime crash prevention and call-flow correctness.

## Cleanup Addendum (same date)

1. `game/status.rpy`
- Navigation action buttons now use script label call (`status_nav_jump`) instead of Python `renpy.jump(...)`.
- Object buttons now dispatch via script flow (`SetVariable("selobj", ...) + Call("onobjsel")`) instead of `Function(qsp_select_object, ...)`.

2. `game/Inn/NextDay.rpy`
- Replaced Python `renpy.jump(retlocname)` with script-level `jump expression` plus fallback.

3. `game/Inn/IntAlberTalk.rpy`
- Removed Python `renpy.jump(...)` in helper actions.
- Converted action result flow to label-return + script jump.
- Removed Python `renpy.call('ShowImageSeq', ...)` usage in punch branch by using callable dispatch.

4. `game/Inn/AmandaDynamicCommonBlocks.rpy`
- Replaced Python `renpy.jump(...)` paths with pending-label handoff (`adc_take_pending_jump()`).

5. `game/Inn/TavernMain.rpy`
- Added pending-label consume step after `CheckIfRunToLegare()` using script `jump expression`.

6. `game/Inn/onobjsel.rpy`
- Replaced `renpy.call_in_new_context(target)` with `renpy.call(target)`.

7. `game/Inn/Intro.rpy`
- Removed `renpy.call_in_new_context(...)` usage from `init_girl()` path.

8. `game/Inn/DebugTools.rpy`
- Replaced `renpy.call_in_new_context("CreateTavernEvents")` with `renpy.call("CreateTavernEvents")`.

9. `game/Inn/DebugTools.rpy` (follow-up)
- Removed Python `renpy.call("CreateTavernEvents")` usage by switching debug builders to `build_tavern_events_queue_python(...)`.

10. `game/Inn/DisplayTavernEventShort.rpy` (follow-up)
- Replaced Python compatibility wrapper implementation that used `renpy.call(...)` with pure-python dequeue helper.

11. `game/Inn/onobjsel.rpy` (follow-up)
- Removed Python `renpy.call(...)` from `qsp_select_object`; helper now only updates selection/deferred handler state.
